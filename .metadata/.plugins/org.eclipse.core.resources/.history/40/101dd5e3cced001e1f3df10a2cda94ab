package Week4;
import java.util.Scanner;

public class 예제_3_12 {
	
	private static void showArray(int[] ar) {
		for (int i = 0; i < ar.length; i++)
		{
			System.out.printf("%2d ", ar[i]);
		}
		
		System.out.println();
		System.out.println("-------------------------");
	}
	
	public static void checker(int size, int[] arr)
	
	public static void setArray(int[] arr, int start, int end)
	{
		for (int i = 0; i < arr.length; i++)
		{
			int randNum = (int)(Math.random()*1000)%(end - start + 1) + start; //나머지 연산은 정수/정수만 가능
			
		}
	}
	
	private static void sortArray(int[] arr) {
		for (int i = 0; i < arr.length; i++)
		{
			//System.out.printf("%d번째 정렬 ", i);
			for (int j = 0; j < arr.length - 1; j++)
			if (arr[j] > arr[j+1])
			{
				int temp = arr[j];
				arr[j] = arr[j + 1];
				arr[j + 1] = temp;
			}
			//정렬 과정을 보고 싶으면 showArray(ar);
		}
	}
	
	public static void main(String[] args)
	{
		int[] ar;
		Scanner input = new Scanner(System.in);
		
		System.out.print("정수 몇개를 입력할 까요?: ");
		int size = input.nextInt();
		
		System.out.print("start: ");
		int start = input.nextInt();
		
		System.out.print("end: ");
		int end = input.nextInt();
		
		ar = new int[size];
		setArray(ar, start, end);
		showArray(ar);
		sortArray(ar);
		showArray(ar);
	}


}
